Create a workflow diagram for a "Cross-Channel Global Feature Pool" algorithm in a visual-inertial navigation system paper. The style should be clean, academic, and suitable for a computer vision / robotics conference paper (e.g., ICRA, IROS, CVPR). Use a two-column layout with clear arrow flows.

## Overview

The figure illustrates how features from multiple polarization channels (S0 intensity, DoP degree of polarization, AoP sin, AoP cos) are merged into unified "global features" across frames.

## Layout Description

### Left side — "Per-Channel Processing" (vertical column)
Show 4 small grayscale camera frames stacked vertically, each labeled:
- S0 (Intensity)
- P0 (DoP)
- P1 (sinAoP)
- P2 (cosAoP)

On each frame, draw 3-4 colored dots representing detected feature points. Use the same color for the same physical feature across channels.

### Center — "Global Feature Pool" (central block)
A rounded rectangle containing three sub-stages arranged horizontally:

**Stage 1: Propagation (Frame k-1 → Frame k)**
Show 2 small icon-like frames (prev and cur). Draw an arrow from a "global feature" (a circle merging 2-3 colored dots) in prev frame to the same global feature in cur frame. Add a label: "A global feature survives if at least one channel still tracks it."

**Stage 2: Association (New local features → existing global features)**
Show small dots entering the pool from the left, with a magnifying glass icon checking spatial distance < 4px. One dot merges into an existing circle (labeled "attach"), another forms a new circle (labeled "new global feature").

**Stage 3: Output (Global features → Backend)**
Show circles (global features) on the right side, each emitting exactly one solid arrow to a "Backend Estimator" box. Add a label: "One observation per global feature per frame."

### Right side — "Backend Estimator"
A rectangular block representing the VINS sliding-window backend. It should look like a simplified factor graph with small square nodes (pose) and circle nodes (landmark). Add a label: "Unaware of channel source."

## Visual Style

- Use a light background (white or very light gray).
- Use a restrained color palette: black/dark gray for outlines, blue tones for S0, green for DoP, red/orange for sinAoP, purple for cosAoP.
- Use arrows (→) for data flow, dashed lines for associations.
- Sans-serif font for labels.
- Include a numbered caption below the figure explaining the three stages.

## Caption

"Cross-channel global feature pool workflow. (a) Propagation: global features from the previous frame persist if at least one associated channel still tracks the point in the current frame. (b) Association: newly detected local features are merged into existing global features if within a spatial distance threshold ($\tau_{\text{dist}} = 4$ px), otherwise a new global feature is created. (c) Output: each global feature submits exactly one observation per frame to the backend estimator, which remains agnostic to the channel source."

Output format: SVG or high-resolution PNG.
